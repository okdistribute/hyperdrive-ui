{
  "name": "dat-explorer",
  "version": "1.0.0",
  "description": "Explore and download a dat in the browser.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify app.js -o bundle.js -t brfs",
    "watch": "watchify app.js -o bundle.js -t brfs",
    "serve": "node cli.js",
    "start": "npm run serve & npm run watch"
  },
  "author": "Karissa McKelvey <karissa@karissamck.com> (http://karissamck.com/)",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^12.0.1",
    "watchify": "^3.6.1"
  },
  "dependencies": {
    "brfs": "^1.4.2",
    "ractive": "^0.7.3",
    "tree-view": "^2.2.0",
    "xhr": "^2.2.0"
  }
}
